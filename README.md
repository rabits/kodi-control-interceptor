# Kodi control interceptor Add-on

This service provides forwarder for the Kodi Web port with intercepting of the jsonrpc calls.

This allows you to disable any call (Like Application.Quit or System.Reboot) and setup script
callback instead (to use Kore buttons to run your scripts).
